import s from './Input.module.css'

import React from 'react';

export const Input = () => {
    return (

            <div>
                <input />
                <button> + </button>
            </div>
    );
};

export default Input;